void main(List<String> args) {
  // list<String> args allows this dart program to accept command line arguments without actually modifying
  // the code, we can just pass the command line arguments to the function as starting point to the function
  print(args);
}
