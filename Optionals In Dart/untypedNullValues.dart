void main(List<String> args) {
  const someValue =
      null; // when we do not specify the data type and assign a variable to nulll then
  // it do not understand the data type and set the data type as dynamic
}
