// OMITTING RETURN TYPES
// there is a linter warning for return type in dart otherwise you can also omit
// but it is not suggested to omit the return type in larger code bases, always use the return type
void main(List<String> args) {
  printHello();
}

printHello() {
  print("helllo");
}
